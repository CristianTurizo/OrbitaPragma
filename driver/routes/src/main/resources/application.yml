server:
  port: 8081

feign:
  microservice:
    users: localhost:8080/usuario

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/OrbitaPragma
    username: root
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
